# ubuntu24-replit
```Install dependencies
mkdir -p .ubuntu24
cd .ubuntu24

wget https://cdimage.ubuntu.com/ubuntu-base/releases/24.04/release/ubuntu-base-24.04-base-amd64.tar.gz

mkdir rootfs
tar -xzf ubuntu-base-24.04-base-amd64.tar.gz -C rootfs
```
```start
chmod +x main.sh

./main.sh
```
username: user
pass: 1234

BTW. might show error or something like that but it will still work
